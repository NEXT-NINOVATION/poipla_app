name: poipla_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_riverpod: ^1.0.3
  go_router: ^3.1.0
  retrofit: ">=3.0.0 <4.0.0"
  freezed_annotation:
  shared_preferences: ^2.0.15
  json_serializable:
  dio: ^4.0.6
  flutter_svg: ^1.0.3
  bordered_text: ^2.0.0
  ruby_text: ^3.0.1
  qr_code_scanner: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  retrofit_generator: ">=4.0.0 <5.0.0"
  build_runner:
  freezed:

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  fonts:
    - family: LightNovelPOPv2
      fonts:
        - asset: assets/fonts/LightNovelPOPv2.otf

  assets:
    - assets/svg/
